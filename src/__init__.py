"""Src package: all application code lives under src/.

Kept as a regular package (not installed) so reviewers can read top to bottom.
Run commands from the repo root, e.g.:
    python -m src.ingest
    uvicorn src.api:app --reload
"""